{
  "name": "promptflux-electron",
  "version": "0.1.0",
  "private": true,
  "description": "PromptFlux desktop app shell",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "electron": "^31.7.7",
    "typescript": "^5.8.2"
  }
}
